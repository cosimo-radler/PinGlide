{
  "name": "pinglide-pinterest-gallery",
  "version": "1.2.0",
  "private": true,
  "description": "A build-free Chrome extension for keyboard navigation on Pinterest.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check src/core.js && node --check src/background.js && node --check src/content.js && node --check popup/popup.js"
  }
}
